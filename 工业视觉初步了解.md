[toc]

# 工业视觉

传统机器视觉检测方案高度依赖人工设计特征算子，针对固定缺陷类型编写检测逻辑，在新能源三电、储能装备、汽车电控、PCB电路板等复杂工业场景中，存在新品换型周期长、未知缺陷识别能力弱、缺陷根因分析能力缺失等短板。随着多模态视觉‑语言大模型（Vision‑Language Large Language Model,V‑LLM）技术迭代，利用通用多模态基座结合行业图文指令微调构建垂类视觉检测模型，已经成为工业质检新的技术路径。

## 之前存在的问题

传统机器视觉方案分为规则式视觉与传统深度学习检测两类：规则视觉依靠阈值、边缘、形态学算法，光照变化、反光、粉尘干扰下稳定性较差；YOLO系列、Mask‑R‑CNN等深度学习检测模型，只能完成目标定位、分类输出，无法给出缺陷机理解释、风险等级、工艺溯源文本结论，输出结果难以直接对接MES、质量追溯系统形成完整质检报告。

以新能源电池包检测为例，产线现场成像条件复杂：强光直射造成金属极柱反光、车间暗光阴影、高温工况下热成像噪声、油污粉尘遮挡、拍摄角度偏移等，固定算法容易产生大规模误检、漏检。每更换一代电芯结构、更新工艺参数，需要算法工程师重新标注大量样本、重新训练检测网络，迭代周期长，难以适配柔性制造快速换产需求。

## 引入新技术

多模态视觉大模型将图像感知与大语言模型语义推理融合，模型可以接收图像输入与自然语言指令，完成缺陷定位、缺陷分类、风险等级判定、故障原因推理、结构化检测报告自动输出，实现“看图+推理+输出专业文本结论”，补齐传统视觉缺少语义推理的短板。基于信息安全，需要使用自有工业图像样本构建图文指令训练集，开展轻量化自训练微调，得到面向自身业务场景的垂类视觉检测模型；在本地服务器或者边缘计算单元完成私有化自推理，形成自主可控的智能视觉检测能力，降低对商业闭源检测系统的依赖。

## 心得

对于工业视觉领域，可以理解为需要看明白存在什么缺陷、缺陷的原因以及风险等级等内容。计算机视觉在不同领域中，有着共同的目标，即需要识别图像中的内容，同时给出从图像中分析出的信息，因此在技术上可互相参考。
同时在细节上又有着不同，例如在工业视觉领域中，是有着明确的生产的环境以及企业安全的要求的，所以需要对图像识别的时候需要排除图像中环境干扰信息（强光直射、暗光、局部遮挡、表面附着粉尘污渍、拍摄参数不同）识别出不同缺陷程度的样本。由于是企业使用，那么需要进行对信息的管理，同时保证信息安全，还需要注意知识产权风险。那么对于企业来讲，是注重经济效益的，所以在架构选择方面，目前成熟的落地方案是混合架构方案（传统检测网络+多模态大模型）。通过使用YOLO、SAM等轻量化传统视觉模型做高速前置筛选，快速定位疑似缺陷区域；将疑似缺陷截图送入多模态垂类模型开展细粒度语义推理、风险判定、报告生成。
像传统检测网络，更轻量，但无法对给出故障原因推理，只适合筛选出疑似缺陷的样本并且定位到对应的区域；多模态垂类模型参数大计算慢，可以给出精细的结果，适合对数据进行筛选之后给出最后的结果。
